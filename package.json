{
  "scripts": {
    "cypress:run": "npx cypress run",
    "test:report": "npx cypress run && npm run allure:generate && allure serve"
  },
  "devDependencies": {
    "cypress": "^13.15.0"
  },
  "dependencies": {
    "cypress-plugin-api": "^2.11.2"
  }
}
